export enum SpellAreaEffectType {
	SPHERE = 'sphere',
	CONE = 'cone',
	CYLINDER = 'cylinder',
	LINE = 'line',
}

export enum SpellComponentType {
	V = 'V',
	S = 'S',
	M = 'M',
}

export enum FilterType {
	ALL = 'ALL',
	FAVOURITE = 'FAVOURITE',
}
